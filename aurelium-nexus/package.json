{
  "name": "aurelium-nexus",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev:web": "cd apps/web && npm install && npm run dev",
    "dev:api": "cd apps/api && pip install -r requirements.txt && uvicorn main:app --reload --port 8000",
    "dev": "npm-run-all --parallel dev:web dev:api"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
